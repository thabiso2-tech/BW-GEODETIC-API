version: '3.8'

services:
  backend:
    image: ${DOCKER_USERNAME}/backend:${VERSION:-latest}
    container_name: bw-geodetic-backend
    ports:
      - "8000:8000"
    environment:
      - APP_NAME=BotswanaGeodeticEngine
      - ENVIRONMENT=production
      - LOG_LEVEL=info
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - geodetic-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  web:
    image: ${DOCKER_USERNAME}/web:${VERSION:-latest}
    container_name: bw-geodetic-web
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://backend:8000
    restart: always
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - geodetic-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  frontend-streamlit:
    image: ${DOCKER_USERNAME}/frontend:${VERSION:-latest}
    container_name: bw-geodetic-frontend
    ports:
      - "8501:8501"
    environment:
      - ENVIRONMENT=production
    restart: always
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - geodetic-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  geodetic-network:
    driver: bridge
